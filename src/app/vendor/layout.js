/* Private / transactional page — kept out of search results. */
export const metadata = { title: 'Vendor Portal', robots: { index: false, follow: false } };

export default function Layout({ children }) {
  return children;
}
