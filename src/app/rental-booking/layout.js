/* Private / transactional page — kept out of search results. */
export const metadata = { title: 'Book a rental', robots: { index: false, follow: false } };

export default function Layout({ children }) {
  return children;
}
